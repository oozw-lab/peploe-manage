<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.czjtxy.sjypeploemanage.mapper.UserMapper">

    <resultMap id="userMap" type="cn.edu.czjtxy.sjypeploemanage.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectByUsername" resultMap="userMap">
        SELECT * FROM `user` WHERE username = #{username}
    </select>

    <select id="selectById" resultMap="userMap">
        SELECT * FROM `user` WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="cn.edu.czjtxy.sjypeploemanage.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, password, nickname, create_time, update_time)
        VALUES (#{username}, #{password}, #{nickname}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="cn.edu.czjtxy.sjypeploemanage.entity.User">
        UPDATE `user`
        <set>
            <if test="nickname != null">`nickname` = #{nickname},</if>
            <if test="email != null">`email` = #{email},</if>
            <if test="phone != null">`phone` = #{phone},</if>
            <if test="password != null">`password` = #{password},</if>
            `update_time` = NOW()
        </set>
        WHERE `id` = #{id}
    </update>

</mapper>